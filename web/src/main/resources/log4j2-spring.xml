<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="60">
    <Properties>
        <Property name="LOG_HOME">logs</Property>
        <Property name="APP_NAME">${spring:application.name:-application}</Property>
        <Property name="LOG_FILE">${LOG_HOME}/${APP_NAME}.log</Property>
    </Properties>

    <SpringProfile name="!prod">
        <Appenders>
            <Console name="Console" target="SYSTEM_OUT">
                <PatternLayout
                        pattern="%style{%d{yyyy-MM-dd HH:mm:ss.SSS}}{cyan}
[%style{%t}{blue}]
%highlight{%-5level}
%notEmpty{%notEmpty{traceId=%style{%X{traceId}}{magenta}}
%notEmpty{tenantId=%style{%X{tenantId}}{yellow}}
%notEmpty{username=%style{%X{username}}{green}}
%notEmpty{url=%style{%X{url}}{bright_white}}
}%style{%c{1.}}{Cyan}: %msg%n"/>
            </Console>
        </Appenders>
        <Loggers>
            <Root level="info">
                <AppenderRef ref="Console"/>
            </Root>
        </Loggers>
    </SpringProfile>

    <SpringProfile name="prod">
        <Appenders>
            <Console name="Console" target="SYSTEM_OUT">
                <JsonLayout
                        complete="false"
                        compact="true"
                        eventEol="true"
                        stacktraceAsString="true"
                        properties="true"/>
            </Console>

            <RollingFile name="FileAppender"
                         fileName="${LOG_FILE}"
                         filePattern="${LOG_HOME}/archive/${APP_NAME}-%d{yyyy-MM-dd}-%i.log.gz">
                <JsonLayout
                        complete="false"
                        compact="true"
                        eventEol="true"
                        stacktraceAsString="true"
                        properties="true"/>

                <!-- 滚动策略：每天 + 文件大小 -->
                <Policies>
                    <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                    <SizeBasedTriggeringPolicy size="100 MB"/>
                </Policies>

                <!-- 最多保留 7 天 -->
                <DefaultRolloverStrategy max="7"/>
            </RollingFile>
        </Appenders>

        <Loggers>
            <Root level="info">
                <AppenderRef ref="Console"/>
                <AppenderRef ref="FileAppender"/>
            </Root>
        </Loggers>

    </SpringProfile>

</Configuration>